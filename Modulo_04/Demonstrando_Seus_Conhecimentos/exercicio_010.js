/*
Crie uma função que receba dois valores, o primeiro será uma palavra qualquer, e o
segundo será a quantidade de vezes que o sistema irá imprimir essa palavra.

Não é necessário executar a função, somente declarar.
*/

function repete(valor, qtd){
    for(i = 0; i< qtd; i++)
        console.log(valor)
}

repete("Teste", 5)